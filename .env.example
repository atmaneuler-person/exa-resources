# Next Auth
# Generate a secret using `npx auth secret` or any random string
AUTH_SECRET=

# Admin Credentials (Optional - for initial login if DB is empty)
ADMIN_EMAIL=admin@exaeuler.com
ADMIN_PASSWORD=exa1234

# Database
# The app uses SQLite by default, so DATABASE_URL is managed internally in lib/prisma.ts
# but you might need it for prisma CLI if you change the schema.
# DATABASE_URL="file:./dev.db"
